# Base URL of the Sevanna API (https://sevanna-api.onrender.com/docs), used
# server-side only (Server Components / generateStaticParams). Optional —
# defaults to the hosted Render instance below if unset.
SEVANNA_API_URL=https://sevanna-api.onrender.com/api/v1
