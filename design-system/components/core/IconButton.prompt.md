Circular icon-only button — pagination, close, next/prev, small toolbar actions.

```jsx
<IconButton name="arrow-right" variant="filled" label="Siguiente" />
<IconButton name="heart" variant="ghost" />
```

Variants: `ghost` (hairline), `gold` (gold outline), `filled` (gold foil). Always pass `label` for accessibility.
