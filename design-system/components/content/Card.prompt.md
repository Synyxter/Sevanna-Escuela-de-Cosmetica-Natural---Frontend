Elevated surface container. Emerald by default; pass `light` for cream surfaces, `hover` for a lift interaction.

```jsx
<Card>Contenido</Card>
<Card light hover>Sobre fondo claro</Card>
<Card padded={false}>Imagen a sangre</Card>
```
