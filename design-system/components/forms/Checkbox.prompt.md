Checkbox with a gold-foil fill and check. Works controlled (`checked` + `onChange`) or uncontrolled (`defaultChecked`).

```jsx
<Checkbox label="Acepto los términos" defaultChecked />
<Checkbox label="Recibir novedades" checked={v} onChange={setV} />
```
