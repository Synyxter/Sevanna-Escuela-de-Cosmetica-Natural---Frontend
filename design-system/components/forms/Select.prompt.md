Dropdown select with a gold chevron, matched to the Input field styling.

```jsx
<Select label="Nivel" options={['Básico','Intermedio','Avanzado']} />
<Select label="Modalidad" options={[{value:'online',label:'Virtual'},{value:'presencial',label:'Presencial'}]} />
```

Accepts plain strings or `{value,label}` objects.
