Single-line text field with an uppercase eyebrow label and inset emerald well. Gold focus ring.

```jsx
<Input label="Correo" type="email" placeholder="tucorreo@ejemplo.com" />
<Input label="Nombre" error="Este campo es obligatorio" />
```

Pass `hint` for helper text, `error` for validation (turns the border red). Set `disabled` to dim.
